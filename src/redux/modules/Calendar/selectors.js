export const api = state => state.Calendar.api;
export const bookings = state => state.Calendar.bookings;
export const selectedDate = state => state.Calendar.selectedDate;
export const fields = state => state.Calendar.fields;

